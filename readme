Проект для синхронизации тарифов Wildberries и выгрузки данных в Google Sheets. 

Вся логика работы по расписанию в docker-compose 

1. Клонировать репозиторий и перейти в папку проекта

2. npm install 

3. docker-compose up

Миграции происходят автоматически в postgres, затем осуществляется выгрузка данных из api wb (каждый час)
Таблицы созданы заранее и листы, так как в бесплатной версиии google workspace нет функционала для создания таблиц и листов.
Идентификаторы гугл таблиц хранятся в коде - хардкод (незя так делать)
По коэффициенту данные сортируются

Из-за нехватки времени, т.к. нахожусь в отпуске , то какие-то моменты я не успел сделать. Например, структурировать правильно файлы в проекте, не хардкодить. 
Спасибо.

